# AMO build artifacts (regardless of migration location)
**/web-ext-artifacts/
**/.amo-upload-uuid

# Local migration and machine-specific data
/.migration-backups/
/.claude/settings.local.json
/themes/local.env
/themes/.generated-local/
/hosts/*/theme.local.env

# OS/editor noise
.DS_Store
nvim.log
